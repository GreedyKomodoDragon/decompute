[package]
name = "decompute-sdk"
version.workspace = true
edition.workspace = true

[features]
default = []
llama = ["dep:decompute-llama"]
llama-metal = ["llama", "decompute-llama/metal"]

[dependencies]
anyhow.workspace = true
decompute-core = { path = "../decompute-core" }
decompute-llama = { path = "../decompute-llama", optional = true, features = ["runtime"] }
tokio.workspace = true
uuid.workspace = true
